import{o as e,c as i,a as l}from"./app.2ac769c5.js";const a='{"title":"Linux 相关","description":"","frontmatter":{},"headers":[{"level":2,"title":"常用命令","slug":"常用命令"},{"level":2,"title":"CentOS 安装 git","slug":"centos-安装-git"},{"level":3,"title":"通过 yum 安装","slug":"通过-yum-安装"},{"level":3,"title":"使用源码编译","slug":"使用源码编译"}],"relativePath":"notes/linux.md","lastUpdated":1632755233785}',t={},d=[l('<h1 id="linux-相关"><a class="header-anchor" href="#linux-相关" aria-hidden="true">#</a> Linux 相关</h1><h2 id="常用命令"><a class="header-anchor" href="#常用命令" aria-hidden="true">#</a> 常用命令</h2><p>查看系统内核版本：<code>uname -a</code></p><h2 id="centos-安装-git"><a class="header-anchor" href="#centos-安装-git" aria-hidden="true">#</a> CentOS 安装 git</h2><h3 id="通过-yum-安装"><a class="header-anchor" href="#通过-yum-安装" aria-hidden="true">#</a> 通过 yum 安装</h3><p>可先通过 <code>yum info git</code> 命令查看 yum 源中 git 的版本，如果满足自己的需求，可直接用过 yum 进行安装</p><div class="language-"><pre><code># yum install -y git  \n</code></pre></div><h3 id="使用源码编译"><a class="header-anchor" href="#使用源码编译" aria-hidden="true">#</a> 使用源码编译</h3><ol><li><p>先卸载低版本的 git</p><div class="language-"><pre><code># yum remove git\n</code></pre></div></li><li><p>安装 git 的依赖库</p><div class="language-"><pre><code># yum install curl-devel expat-devel gettext-devel openssl-devel zlib-devel gcc perl-ExtUtils-MakeMaker\n</code></pre></div></li><li><p>下载所需版本的 git 源码包</p><p>下载地址 <code>https://mirrors.edge.kernel.org/pub/software/scm/git/</code> 或 <code>https://github.com/git/git/releases</code></p><div class="language-"><pre><code># wget https://github.com/git/git/archive/v2.28.0.tar.gz\n</code></pre></div></li><li><p>解压</p><div class="language-"><pre><code># tar -zxvf git-2.28.0.tar.gz\n</code></pre></div><p>这里是解压到当前目录，也可在后面指定目录</p></li><li><p>进入目录，编译安装</p><div class="language-"><pre><code># cd git-2.28.0\n# ./configure --prefix=/usr/local/git\n# make &amp;&amp; make install\n</code></pre></div></li><li><p>配置环境变量</p><p>在 <code>/etc/profile</code> 文件末尾加上一行 <code>export PATH=&quot;/usr/local/git/bin:$PATH&quot;</code>。然后让配置生效：</p><div class="language-"><pre><code># source /etc/profile\n</code></pre></div><blockquote><p>注意：这样配置的是临时环境变量，只在当前shell 环境中有效，重启 shell 后会失效。</p></blockquote><p>或者配置软连接:</p><div class="language-"><pre><code># ln -s /usr/local/git/bin/git /usr/local/bin\n</code></pre></div></li><li><p>检查是否安装成功</p><div class="language-"><pre><code># git --version\n</code></pre></div></li></ol>',9)];t.render=function(l,a,t,r,c,o){return e(),i("div",null,d)};export{a as __pageData,t as default};
